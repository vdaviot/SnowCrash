#!/usr/bin/python
import sys
def resolve(arg):
	resolved = ""
	for i, j in enumerate(arg):
		dec = ord(c) - i
		resolved += chr(dec)
	return resolved

if __name__ == '__main__':
	print sys.argv[1]
	print resolve(sys.argv[1])
